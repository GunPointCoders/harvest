coverage:
  range: 80..100
  status:
    project:
      default: off
      lib:
        flags: lib
      

flags:
  # filter the folder(s) you wish to measure by that flag
  mobx:
    # only include files in the mobx folder
    paths:
      - lib
 
